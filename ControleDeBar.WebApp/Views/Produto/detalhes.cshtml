@model ControleDeBar.WebApp.Models.DetalhesProdutoViewModel

@{
    Layout = "_Layout";
    ViewBag.Title = "Visualizar Detalhes do Produto";
}

<div class="row mt-3">
    <div class="col-lg-6 d-grid gap-2 d-md-block">

        <div class="d-grid">
            <label class="fs-5"><strong>Produto:</strong> @Model.Nome</label>

            <label><strong>Id:</strong> [@Model.Id]</label>
            <label><strong>Valor:</strong> @Model.Valor.ToString("C2")</label>
        </div>

        <a href="/produto/listar" class="btn btn-secondary px-5 mt-4">
            <i class="bi bi-arrow-return-left me-1"></i>
            Voltar
        </a>
    </div>
</div>